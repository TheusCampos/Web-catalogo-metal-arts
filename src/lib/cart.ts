/**
 * @deprecated Use `useCartStore` de "@/stores/cart.store" diretamente.
 * Este arquivo mantém compatibilidade retroativa enquanto a migração ocorre.
 */
export { useCartStore as useCart, type CartItem } from "@/stores/cart.store";
