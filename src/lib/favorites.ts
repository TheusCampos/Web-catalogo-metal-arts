/**
 * @deprecated Use `useFavoritesStore` de "@/stores/favorites.store" diretamente.
 * Este arquivo mantém compatibilidade retroativa enquanto a migração ocorre.
 */
export { useFavoritesStore as useFavorites } from "@/stores/favorites.store";
